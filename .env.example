# Copy to .env and fill in. Vagrant does not auto-source .env; use direnv,
# or `set -a; source .env; set +a; vagrant up`.

# Reusable, ephemeral Tailscale auth key.
# Generate: https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=tskey-auth-REPLACE_ME
